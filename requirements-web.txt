# Web interface dependencies (not covered by privacy-filter/pyproject.toml).
# Gradio 6.15.2 fixes the tab-switching browser freeze (Svelte 5
# effect_update_depth_exceeded regression introduced in 6.11.0).
gradio==6.15.2
PyMuPDF
python-docx
